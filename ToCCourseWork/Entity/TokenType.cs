
namespace ToCCourseWork.Entity
{
    public enum TokenType
    {
        LAMBDA,
        NUMBER,
        IDENTIFIER,
        EQUAL,
        PLUS_OPERATION, //+-
        MULTIPLICATION_OPERATION, // */
        OPEN_BRACKET,    // (
        CLOSE_BRACKET,   // )
        COLON, //:
        COMMA, //,
        END, //;
        INVALID,
        WHITESPACE,
    }
}
